// This file is used by `flutter test`.
// It is intentionally left minimal.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('WeatherData model parses Open-Meteo response', () {
    // Basic smoke test — extend with real tests as needed
    expect(1 + 1, 2);
  });
}
